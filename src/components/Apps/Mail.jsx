import React from 'react';
import PropTypes from 'prop-types';

Mail.propTypes = {
    
};

function Mail(props) {
    return (
        <div>
            
        </div>
    );
}

export default Mail;