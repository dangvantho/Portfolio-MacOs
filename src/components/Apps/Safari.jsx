import React from 'react';
import PropTypes from 'prop-types';

Safari.propTypes = {
    
};

function Safari(props) {
    return (
        <div>
            Safari
        </div>
    );
}

export default Safari;